#include "GameStates.h"

MainScreenState::MainScreenState(Game* game)
	: game(game) {};

void MainScreenState::RenderDraw(SDL_Renderer* renderer) const
{
	//game.bird.IdleFly(deltaTime, nowTicks);
	//game.flappyBirdBanner.SetCenterY(bird.CenterY());
	//game.background.Update(deltaTime);
	//game.ground.Update(deltaTime);
}
